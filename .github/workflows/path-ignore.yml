name: Test Paths Behavior

on:
  push:
    branches:
      - main
    paths:
      - 'gh-CLI-commands'
      - '!src/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "✅ This workflow ran because a valid file changed."
